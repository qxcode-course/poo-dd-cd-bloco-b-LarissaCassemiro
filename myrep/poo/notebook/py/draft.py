class Notebook:
    def __init__(self, bateria, carregador: int):
        self. __bateria(bateria)
        self.__carregador(carregador)

    def usar(self):
                
    def ligar(self):
    if self.bateria != None and
    self.bateria.get_carga() > 0:
    self. ligado = True 

